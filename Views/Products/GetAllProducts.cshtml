@model IEnumerable<ComputerStoreClassLib.Model.Product>

@{
    ViewBag.Title = "GetAllProducts";
}

<h2>GetAllProducts</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
          Фирма поситавщик
        </th>
        <th>
           Категория товара
        </th>
        <th>
           Операционная система
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CountAll)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Rating)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
           Фирма поставщик
        </td>
        <td>
           Категория товара
        </td>
        <td>
            Операционная система
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Price)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AmmountProducts)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Rating)
        </td>
        <td>
            @Html.ActionLink("Add into basket", "AddToBasket", new { id=item.Id }) |
            @Html.ActionLink("Details", "UserDetails", new { id=item.Id }) 
        </td>
    </tr>
}

</table>
